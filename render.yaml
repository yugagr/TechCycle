services: 
  - type: web
    name: techcycle-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    workingDirectory: backend